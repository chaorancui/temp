[toc]

# 文本类

## Beyond Compare

一、**每行字符限制**

菜单栏里：【工具(T)】 $\Longrightarrow$ 【文件格式(F)】 $\Longrightarrow$ 弹出框【转换】标签页里 $\Longrightarrow$ 【每行字符限制】 的字样 $\Longrightarrow$ 修改前面的值(默认是 4096) $\Longrightarrow$ 【保存】。

二、**十六进制每行32个数**

1. 菜单栏选择：
   `视图` → `布局`
2. 弹出界面中：
   - `每组字节数`：改为 8
   - `每项组数`：改为 4
   - 不勾选 `适合窗口`
3. 其余可不修改。
4. 点击 `确定`。

二、**自定义颜色主题**

1. 菜单栏选择：
   `工具 (Tools)` → `选项 (Options)`（或 `Ctrl+Alt+O`）
2. 在左侧选择：
   `颜色 → 文件视图 → 文本比较`（或英文界面：`Colors → File Views → Text Compare`）
3. 右侧是“元素颜色”的列表，找到以下项目：
   - `相同文本 (Matched Text)`（英文为 `Matched Text`）→ 设置为灰色（如 `#888888` 或更浅）
   - `差异文本（Difference Text）` → 保持为红色或其他你喜欢的对比色
4. 你可以点击“编辑主题”按钮（Edit Theme），保存为一个新主题名，例如 `My Gray Match`。
5. 应用并确定。

## Notepad++

一、**使用 FTP 远程**

参考链接：[notepad++的 NppFTP 插件远程连接 linux 操作系统](https://blog.csdn.net/yuanyedage/article/details/108312205)

二、**标记或高亮子字符串**

在 Notepad++ 中标记或高亮单词的一部分（子字符串），最有效的方法是使用“标记”功能（Mark）或搜索功能。
您可以手动输入要标记的字符，并勾选“不区分大小写”或使用正则表达式进行精确匹配，标记后的内容会以特殊颜色突出显示。

**具体操作步骤：**

1. **打开标记窗口：** 按 `Ctrl + M` 或点击菜单栏的 **搜索(Search)** -> **标记(Mark)**。

2. **输入内容：** 在“查找内容(Find what)”框中输入您想要标记的单词的一部分。

3. **配置标记：**
   - 按需勾选 **标记所在行**（行号会显示圆形标记，个人不喜欢）。
   - 按需勾选 **清除上次标记**。
   - 按需勾选 **匹配大小写**。
   - 确保勾选 **循环查找**。

4. **查找模式：**
   - 根据需要选择 **普通** 或 **正则表达式**。

5. **执行标记：** 点击 **全部标记(Mark All)**。

**快捷标记功能：**

- **双击选择：** 如果是连续字符串，可双击直接选中，默认情况下，Notepad++ 会自动高亮显示文件中所有相同的该字符串。

- **清除标记：** 点击菜单栏 **搜索(Search)** -> **清除所有标记(Clear all marks)**。

通过以上方法，您可以快速定位和标记长代码或文章中的特定字符串片段。

## Typora

**Typora_Unlocker**

<https://github.com/743859910/Typora_Unlocker>

<https://www.ghxi.com/typora.html>

**主题：**

- [最美 Markdown 主题 Phycat-color 发布！](https://github.com/sumruler/typora-theme-phycat)

# 二进制

## HexPatch

HexPatch 是一款具有终端用户界面 (TUI) 的二进制修补程序和编辑器，能够反汇编指令和组装补丁。它支持多种架构和文件格式。此外，它还可以通过 SSH 编辑远程文件。

查看[GitHub Pages](https://etto48.github.io/HexPatch/)了解更多信息和一些截图。

# 文件管理

## Total Commander

果核剥壳 xx 版下载：[Total Commander v11.51 增强版 - 果核剥壳](https://www.ghxi.com/totalcommanderpc.html?btwaf=54807558)（找最新版本）

参考链接：

1. [【Win10 软件】Total Commander 基本配置及使用（整理）](https://www.cnblogs.com/sjsea/p/13213210.html)
2. [在 Total Commander 下使用 sftp 操作服务器](https://zhuanlan.zhihu.com/p/26090856)
3. [Total Commander 显示文件包含文件名扩展](https://cloud.tencent.com/developer/article/1446472)

# 画图类

## excalidraw

- [Obsidian-Excalidraw 功能手册](https://zhuanlan.zhihu.com/p/658366428)

在 Excalidraw 中对图像进行旋转和翻转操作非常简单，具体可以通过界面工具或右键菜单快速完成。

**一、图像的旋转**

1. 单击选中画布上的图像。
2. 找到图像顶部或外围边缘的**旋转锚点**（通常是一个独立的小圆圈或小图标）。
3. 按住鼠标左键并拖动，即可自由旋转图像。
   - **快捷技巧**：在拖动旋转时按住 `Shift` 键，可以按 15° 的固定角度进行吸附旋转，非常适合摆正图片。

**二、图像的翻转（水平/垂直镜像）**

1. 单击选中画布上的图像。
2. **右键点击**该图像打开上下文菜单。
3. 在菜单中选择你需要的翻转方式：
   - **水平翻转**：选择 **Flip horizontal**（水平镜像）
   - **垂直翻转**：选择 **Flip vertical**（垂直镜像）

# 终端类

## WezTerm

**配置**

可以参考[Wez's Terminal Emulator ---- Configuration](https://wezterm.org/config/files.html)

在 Windows 的 WezTerm 配置文件中(通常是 `%USERPROFILE%\.wezterm.lua`)，如`C:\Users\cui\.wezterm.lua`:

```lua
-- Pull in the wezterm API
local wezterm = require 'wezterm'

-- This will hold the configuration.
local config = wezterm.config_builder()


-- 设置字体类型
-- 列出可供 WezTerm 使用的所有字体，命令:wezterm ls-fonts --list-system
-- config.font = wezterm.font 'JetBrains Mono'
config.font = wezterm.font("CommitMono Nerd Font Mono", { weight = "Regular", stretch = "Normal", style = "Normal" })

-- 设置字体大小（数字，单位为点/pt）
config.font_size = 14.0

-- Finally, return the configuration to wezterm:
return config=
```

# 下载类

## IDM

关闭 IDM (internet download manager) 自动弹出自动更新的提示
<https://blog.csdn.net/qq_29856169/article/details/137630040>

# AI 类

## opoencode

Toggle session scrollbar

**一、滚动条**

你提到的这两个问题在 OpenCode 的终端界面（TUI）中很常见，也都有对应的解决方法。

1. 如何显示滚动条

   OpenCode TUI 的滚动条默认是**隐藏**的，需要手动开启。
   - **方法一：通过命令面板开启（最直接）**
     1. 按下 `Ctrl+P` 打开命令面板。
     2. 输入并选择 **`Toggle session scrollbar`**。
     3. 滚动条就会显示出来，之后就可以用鼠标拖拽滚动了。
   - **方法二：修改配置文件**
     可以编辑 KV 存储文件来永久开启：
     1. 找到配置文件 `~/.local/state/opencode/kv.json`。
     2. 将 `"scrollbar_visible": false` 改为 `"scrollbar_visible": true`。

   > 如果你使用的是较新版本（v1.4.3+），滚动条可能已默认开启。另外，某些终端模拟器（如 Ghostty）可能原生支持滚动，但如果遇到问题，建议使用上述方法在 OpenCode 应用层开启。

2. 键盘快捷键：不依赖鼠标的滚动方式

   即使没有滚动条，也可以使用快捷键高效浏览：

   | 操作                    | 快捷键                      |
   | ----------------------- | --------------------------- |
   | **向上/下滚动一行**     | `Ctrl+Alt+Y` / `Ctrl+Alt+E` |
   | **向上/下滚动半页**     | `Ctrl+Alt+U` / `Ctrl+Alt+D` |
   | **向上/下滚动一页**     | `PageUp` / `PageDown`       |
   | **跳转到对话开头/结尾** | `Ctrl+G` / `Ctrl+Alt+G`     |

**二、显示历史对话行数**

这确实不是会话列表的问题，而是 OpenCode 为了性能，**默认只加载最近 100 条消息渲染到屏幕上**。终端自身的滚动条只能滚动 OpenCode 已经渲染出来的内容，没加载的（100条之前的）自然滚不到。
针对这个痛点，有以下几个立竿见影的解决方法：

1. 一次性加载全部历史（解决"看不到开头"）

   在当前 TUI 界面中，用鼠标**滚轮向上滚到消息的最顶端**，或者按 `Ctrl+G` 直接跳到开头。
   此时，屏幕顶端会出现一个 **"Load more messages"** 的横幅。点击它，选择 **`Load full session history`**，就能把当前对话从头到尾的全部内容加载进屏幕缓存。加载后，你就可以随意滚动到最初的那条对话了。

2. 高效滚动快捷键（不用鼠标拖）

   配合加载更多历史后，用键盘翻页比鼠标拖拽快得多：
   - `PageUp` / `PageDown`：整页翻动
   - `Ctrl+Alt+U` / `Ctrl+Alt+D`：半页翻动
   - `Ctrl+G`：跳转到对话最开头
   - `Ctrl+Alt+G`：跳转到对话最末尾
